# Overrides to the default environment for testing purposes

# Database config details for sequelize
# (sqlite)
DB_USERNAME=
DB_PASSWORD=
DB_DATABASE="local_webmakers"
DBOPTIONS_DIALECT="sqlite"
DBOPTIONS_PORT=
DBOPTIONS_STORAGE="logintest.sqlite"
DBOPTIONS_LOGGING=false
